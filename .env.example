# Environment Configuration Example
# Copy this to .env.development, .env.staging, or .env.production
# and update the values for each environment

# Environment - development, staging, or production
VITE_ENVIRONMENT=development

# Backend API URL (changes per environment)
VITE_API_URL=http://localhost:3000

# Debug Mode (enabled in dev, disabled in staging/prod)
VITE_DEBUG=true

# Optional: Environment names for tracking services
VITE_SENTRY_ENVIRONMENT=development
VITE_POSTHOG_ENVIRONMENT=development

# ---------------------------------------------------------
# Shared Configuration (same across all environments)
# Add these to a .env.local file (gitignored) if needed
# ---------------------------------------------------------

# Sentry Error Tracking (optional)
# VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# VITE_SENTRY_ORG=your-sentry-org
# VITE_SENTRY_PROJECT=your-frontend-project
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# PostHog Analytics (optional)
# VITE_POSTHOG_API_KEY=phc_your_posthog_api_key
# VITE_POSTHOG_HOST=https://us.i.posthog.com
